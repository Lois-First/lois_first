# C++14-17 新特性与标准库详解

本文以现有 C++11 总结为基线，只记录 C++14 和 C++17 的新增或重要增强。示例默认已包含所需头文件，完整程序还需 `main`。推荐编译参数：

```text
g++ -std=c++17 -Wall -Wextra -pedantic file.cpp
```

## 1. 版本总览

| 标准 | 核心方向 | 语言代表 | 标准库代表 |
|---|---|---|---|
| C++14 | 让 C++11 更易用 | 泛型 Lambda、返回值推导、变量模板、放宽 `constexpr` | `make_unique`、`exchange`、`integer_sequence` |
| C++17 | 提高表达力和编译期能力 | 结构化绑定、`if constexpr`、折叠表达式、模板参数推导 | `optional`、`variant`、`string_view`、文件系统、并行算法 |

## 2. C++14 语言特性

### 2.1 泛型 Lambda

Lambda 参数可以写成 `auto`，编译器会生成带模板调用运算符的闭包。它用于“算法相同、参数类型不同”的小型操作。

```cpp
auto add = [](auto a, auto b) { return a + b; };
int i = add(1, 2);
double d = add(1.5, 2.5);
```

每种参数组合都会产生一个模板实例，返回类型也可能不同，因此泛型 Lambda 不能直接转换成普通函数指针。

### 2.2 Lambda 初始化捕获

初始化捕获能在闭包内创建新成员，常用于移动捕获、改名和计算初值。C++11 的 `[x]` 只能拷贝已有名字，不能捕获只能移动的 `unique_ptr`。

```cpp
auto ptr = std::make_unique<int>(42);
auto task = [base = 10, p = std::move(ptr)] { return base + *p; };
// ptr 此后为空，资源归 task 所有
```

### 2.3 函数返回值类型推导

普通函数可使用 `auto` 推导返回类型。所有参与推导的 `return` 必须得到同一类型；递归调用出现在首个可推导 `return` 之前时，仍需显式返回类型。

```cpp
auto square(int x) { return x * x; }             // int
auto greet() { return std::string{"hello"}; }   // std::string
```

### 2.4 `decltype(auto)`

`auto` 通常丢弃引用和顶层 `const`；`decltype(auto)` 按 `decltype` 规则精确保留它们，适合包装访问函数。

```cpp
decltype(auto) front(std::vector<int> &values) {
    return (values.front()); // int&；括号使左值规则明确
}
```

不要返回局部变量的引用。`decltype(auto) f(){ int x; return (x); }` 会产生悬空引用。

### 2.5 变量模板

变量也可以被类型或值参数化。典型用途是数学常量和 C++17 的类型特征 `_v` 简写。

```cpp
template<class T>
constexpr T pi = T(3.1415926535897932385L);

static_assert(pi<double> > 3.14, "pi");
```

### 2.6 放宽的 `constexpr`

C++14 的 `constexpr` 函数可包含局部变量、循环、分支和多条语句，使编译期算法不再局限于单个 `return`。

```cpp
constexpr int factorial(int n) {
    int result = 1;
    for (int i = 2; i <= n; ++i) result *= i;
    return result;
}
static_assert(factorial(5) == 120, "compile-time result");
```

当实参不是常量表达式时，`constexpr` 函数仍可作为普通运行时函数调用。

### 2.7 字面量与属性

- 二进制字面量：`0b1111'0000u`。
- 数字分隔符：`1'000'000`，单引号不影响数值。
- `[[deprecated]]` 或 `[[deprecated("reason")]]`：标记弃用接口，编译器通常在调用点警告。

```cpp
[[deprecated("use connect_v2")]]
void connect_v1();
```

## 3. C++14 标准库

### 3.1 `std::make_unique`

安全构造 `unique_ptr`，不用重复写类型，并避免复杂表达式中的资源泄漏风险。

```cpp
auto one = std::make_unique<int>(7);       // unique_ptr<int>
auto many = std::make_unique<int[]>(100); // unique_ptr<int[]>
```

### 3.2 `std::exchange`

把对象替换成新值并返回旧值。常用于移动构造函数将源对象重置。

```cpp
Socket(Socket &&other) noexcept
    : handle_(std::exchange(other.handle_, invalid_handle)) {}
```

### 3.3 编译期整数序列

`integer_sequence<T, values...>` 保存编译期整数包；`index_sequence<I...>` 使用 `size_t`。`make_index_sequence<N>` 生成 `0..N-1`，常用于展开 tuple。

```cpp
template<class Tuple, std::size_t... I>
void print_tuple(const Tuple &t, std::index_sequence<I...>) {
    using swallow = int[];
    (void)swallow{0, ((std::cout << std::get<I>(t) << ' '), 0)...};
}
```

### 3.4 其他库增强

- `std::quoted`（`<iomanip>`）：以引号输出字符串，读取时保留空格并处理转义。
- `std::shared_timed_mutex`：允许多读者或单写者，并支持超时锁定。
- 关联容器异构查找：比较器使用 `std::less<>` 等透明比较器时，`find` 可直接接受不同但可比较的类型，减少临时对象。
- `std::tuple` 增加按类型 `get<T>`；要求该类型在 tuple 中恰好出现一次。

```cpp
std::ostringstream out;
out << std::quoted("hello world"); // 输出 "hello world"
```

## 4. C++17 语言特性

### 4.1 结构化绑定

把数组、`pair`/`tuple` 或数据成员可访问的对象分解成多个名字。使用 `auto` 会创建相应对象，使用 `auto&`/`const auto&` 可避免拷贝。

```cpp
std::map<std::string, int> scores{{"Ada", 10}};

for (const auto &[name, score] : scores)
    std::cout << name << ": " << score << '\n';

auto [it, inserted] = scores.insert({"Bob", 8});
```

结构化绑定的名字不是普通引用变量的简单语法糖，其具体类型规则依赖被分解对象；通常用 `const auto&` 遍历即可。

### 4.2 `if` 和 `switch` 初始化语句

在条件前声明临时变量，作用域只覆盖条件和分支，适合迭代器、锁、解析结果。

```cpp
if (auto it = scores.find("Ada"); it != scores.end()) {
    std::cout << it->second;
}
```

分号左侧是初始化语句，右侧才是条件。变量在 `else` 中仍然可见。

### 4.3 `if constexpr`

编译期选择模板分支。未选中的分支不会实例化，因此可以放置对另一类类型不合法的代码。

```cpp
template<class T>
std::size_t length(const T &value) {
    if constexpr (std::is_array_v<T>)
        return std::size(value);
    else
        return value.size();
}
```

它不是预处理器：语法仍要合法，且模板之外被丢弃分支仍需通过普通语义检查。

### 4.4 折叠表达式

用一个运算符展开参数包，取代许多递归模板。形式包括一元左折叠 `(... op args)`、一元右折叠 `(args op ...)`，以及带初值的二元折叠。

```cpp
template<class... Args>
auto sum(Args... args) { return (args + ...); }

template<class... Args>
void print(const Args &...args) {
    (std::cout << ... << args) << '\n';
}
```

注意非交换运算符的左右方向。空参数包只有 `&&`、`||`、`,` 的一元折叠有标准定义值；其他运算应提供初值。

### 4.5 类模板参数推导（CTAD）

构造类模板对象时可从构造实参推导模板参数。标准库为常用模板提供推导指引，也可自定义 `Template(args) -> Template<types...>`。

```cpp
std::pair pair{1, 2.5};       // pair<int, double>
std::vector values{1, 2, 3};  // vector<int>

template<class T> struct Box { Box(T) {} };
Box(const char *) -> Box<std::string>;
Box text{"hello"};            // Box<string>
```

CTAD 只用于变量构造，不用于函数参数类型；接口里仍要写完整类型或使用模板。

### 4.6 `auto` 非类型模板参数

编译器同时推导非类型模板参数的类型和值，适合常量包装和策略配置。

```cpp
template<auto Value>
struct constant {
    static constexpr auto value = Value;
};

static_assert(constant<42>::value == 42, "value");
```

### 4.7 内联变量

`inline` 变量可在头文件定义，并在多个翻译单元中表示同一实体。命名空间级常量、注册对象和类静态成员因此更易组织。

```cpp
inline constexpr int max_connections = 64;

struct Limits {
    static constexpr int max_items = 100; // C++17 不再需要类外定义
};
```

### 4.8 保证的复制消除

某些场景中纯右值直接构造目标对象，不要求类型具有可用的拷贝/移动构造函数。

```cpp
struct Token {
    Token() = default;
    Token(const Token &) = delete;
    Token(Token &&) = delete;
};

Token make_token() { return Token{}; } // C++17 保证可行
```

`return named_local;` 的 NRVO 仍不是强制保证；只有直接返回同类型纯右值等规定场景受保证。

### 4.9 Lambda 增强

- Lambda 可隐式成为 `constexpr`，满足常量表达式要求时可在编译期执行。
- `[*this]` 按值捕获当前对象，而 `[this]` 只捕获指针；异步执行时前者更易控制生命周期。
- 泛型 Lambda 可配合 `if constexpr` 编写类型分支。

```cpp
constexpr auto square = [](int x) { return x * x; };
static_assert(square(5) == 25, "lambda");
```

### 4.10 新属性和语法便利

| 特性 | 作用 |
|---|---|
| `[[nodiscard]]` | 忽略函数返回值时请求诊断，适合错误结果和资源工厂 |
| `[[maybe_unused]]` | 表明实体可能有意不使用 |
| `[[fallthrough]]` | 标记 `switch` 中有意贯穿到下一标签 |
| `namespace A::B {}` | 简写嵌套命名空间 |
| `static_assert(expr);` | 断言消息可省略 |
| `using enum` | 不是 C++17，而是 C++20，迁移时不要误用 |

### 4.11 其他值得知道的规则变化

- `noexcept` 成为函数类型的一部分，函数指针转换会考虑异常规范。
- 表达式求值顺序进一步明确，例如 `a().b().c()` 左到右，函数实参彼此完整求值但相互顺序仍未指定。
- 支持过度对齐对象的 `new`，标准库用 `std::align_val_t` 表示对齐参数。
- 聚合初始化放宽，派生类在满足条件时也可作为聚合。
- 枚举可直接初始化：`enum class Byte : unsigned char {}; Byte b{42};`。

## 5. C++17 标准库

### 5.1 `std::optional<T>`：可能没有值

用于表达“值存在或不存在”，替代魔法值和不安全的输出参数。`has_value()`/`operator bool` 查询；`value()` 取值（空时抛 `bad_optional_access`）；`value_or(fallback)` 提供默认值；`reset()` 清空；`emplace(args...)` 原位构造。

```cpp
std::optional<int> parse_port(std::string_view text) {
    if (text.empty()) return std::nullopt;
    try { return std::stoi(std::string{text}); }
    catch (const std::exception &) { return std::nullopt; }
}

int port = parse_port("8080").value_or(80);
```

不要让 `optional<string_view>` 指向临时字符串；optional 只管理 view 对象，不管理其字符存储。

### 5.2 `std::variant<Ts...>`：类型安全联合体

同一时刻保存一个备选类型，自动管理对象生命周期。`std::get<T/I>` 取值；`get_if` 返回指针；`holds_alternative<T>` 查询；`visit` 根据当前类型分派。类型不匹配的 `get` 抛 `bad_variant_access`。

```cpp
std::variant<int, std::string> value = std::string{"ok"};
std::visit([](const auto &x) { std::cout << x << '\n'; }, value);
if (auto p = std::get_if<std::string>(&value)) p->append("!");
```

### 5.3 `std::any`：任意可拷贝类型

运行时类型擦除容器。`has_value()` 查询是否有对象；`type()` 返回 `type_info`；`any_cast<T>` 取出值，失败抛 `bad_any_cast`（指针重载失败返回 `nullptr`）。相比 `variant` 更灵活，但失去编译期备选类型检查并可能产生分配。

```cpp
std::any data = 42;
if (const int *p = std::any_cast<int>(&data)) std::cout << *p;
```

### 5.4 `std::string_view`：非拥有字符串视图

只保存字符指针和长度，不分配、不拷贝。常用函数：`size`、`empty`、`substr`、`find`、`compare`、`remove_prefix`、`remove_suffix`。底层字符串必须比 view 活得久。

```cpp
bool starts_with(std::string_view text, std::string_view prefix) {
    return text.size() >= prefix.size() &&
           text.substr(0, prefix.size()) == prefix;
}
```

### 5.5 文件系统 `<filesystem>`

`path` 表示路径并负责平台分隔符；`exists`、`is_regular_file`、`is_directory`、`file_size` 查询状态；`create_directories` 创建目录；`directory_iterator` 遍历；`copy`、`rename`、`remove` 执行修改。多数函数有抛异常和接收 `error_code` 的非抛出重载。

```cpp
namespace fs = std::filesystem;
for (const fs::directory_entry &entry : fs::directory_iterator("."))
    std::cout << entry.path().filename().string() << '\n';
```

### 5.6 并行算法 `<execution>`

`seq` 顺序执行，`par` 可使用多个线程，`par_unseq` 允许并行且向量化。可传给 `sort`、`for_each`、`transform`、`reduce` 等算法。

```cpp
std::sort(std::execution::par, values.begin(), values.end());
```

谓词和操作必须避免数据竞争、阻塞和依赖调用顺序；并行策略不保证一定比顺序版本快。

### 5.7 数值转换 `<charconv>`

`to_chars` 和 `from_chars` 无分配、与区域设置无关，适合高性能序列化。返回结构体含指针和 `std::errc` 错误码。

```cpp
char buffer[32];
auto result = std::to_chars(buffer, buffer + sizeof buffer, 12345);
if (result.ec == std::errc{})
    std::cout.write(buffer, result.ptr - buffer);
```

### 5.8 容器节点句柄与插入

关联容器的 `extract` 取出节点而不析构元素，`node_handle` 可修改键后插入另一个兼容容器；`merge` 批量转移节点。适合重组 map/set，避免复制和重新分配。

```cpp
std::map<int, std::string> a{{1, "one"}}, b;
auto node = a.extract(1);
node.key() = 2;
b.insert(std::move(node));
```

### 5.9 其他标准库新增/增强

- `std::gcd`、`std::lcm`（`<numeric>`）：整数最大公约数和最小公倍数。
- `std::clamp`（`<algorithm>`）：把值限制在闭区间 `[lo, hi]`。
- `std::size`、`std::empty`、`std::data`（`<iterator>`）：统一处理容器、数组和字符串字面量。
- `std::inclusive_scan`、`exclusive_scan`、`transform_reduce`：可配合执行策略进行前缀和与归约。
- `std::byte`（`<cstddef>`）：表达原始内存字节，不参与整数算术；使用 `to_integer` 转回整数。
- `std::pmr` 多态内存资源（`<memory_resource>`）：运行时选择分配器，提供 `pmr::vector`、`pmr::string` 等别名。
- `std::shared_lock`（C++14）、`shared_mutex`（C++17）支持多读者/单写者；`scoped_lock`（C++17）可一次锁定多个互斥量并避免常见死锁。
- `map::try_emplace`：键不存在时才构造值；`insert_or_assign`：不存在则插入，存在则赋值。
- `std::apply`：用 tuple 展开调用函数；`std::invoke`：统一调用函数、成员函数、成员指针和函数对象。
- `std::not_fn`：生成对可调用对象结果取反的包装器，通常比 `not1/not2` 更通用。
- `enable_shared_from_this::weak_from_this`：无需增加引用计数即可取得指向自己的 `weak_ptr`。
- `std::uncaught_exceptions()`：返回当前未处理异常数量，替代只能返回布尔值的旧接口。
- `basic_string::data()` 新增非常量重载，可安全取得连续、可写的字符缓冲区（不能越过 `size()`）。

```cpp
int bounded = std::clamp(value, 0, 100);
int common = std::gcd(84, 30); // 6
```

```cpp
std::map<std::string, Widget> widgets;
widgets.try_emplace("main", constructor_arg); // 键已存在时不构造 Widget
widgets.insert_or_assign("main", Widget{});    // 插入或覆盖

auto args = std::make_tuple(2, 3);
int product = std::apply([](int a, int b) { return a * b; }, args);
```

## 6. 迁移建议与陷阱

- “可能没有值”用 `optional`；“互斥的几种类型”用 `variant`；只有运行时完全未知类型时才用 `any`。
- 只读函数参数优先 `string_view`，但不要从临时 `std::string` 创建长期 view。
- `if constexpr` 与折叠表达式通常可替代 C++11 的递归模板和复杂 SFINAE。
- `make_unique`、`optional`、`variant`、`string_view` 都遵循值语义，但不改变迭代器失效、异常安全和对象生命周期规则。
- 旧版 GCC 使用 `<filesystem>` 可能需要额外链接 `-lstdc++fs`；并行算法需要实现提供线程后端。
- `std::move` 仍只是转换，不保证真的移动；移动后的对象只能依赖其类型规定的有效状态。
- `volatile` 不是线程同步工具；跨线程共享请用 `std::atomic`、互斥量或消息传递。
